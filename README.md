## yoga APP

To run this 

install poetry locally 

```
poetry shell 
poetry install 

uvicorn yoga_app.main:app --reload       

```

go to server and do 

```

git pull origin main

docker compose down 
docker compose up -d --build

```
